# === CREDENTIALS & SECRETS ===
.env
# Specific sensitive JSON files only (NOT all *.json — workflow/test JSONs must be versioned)
*token*.json
*secret*.json
*credentials*.json
*auth*.json
*key*.json
# Gmail OAuth — explicit paths
agentes/email_manager/tools/jarvis_token.json
agentes/email_manager/tools/client_secret*.json
tools/jarvis_token.json
tools/client_secret*.json

# === SENSITIVE BUSINESS DATA ===
# Historical memory and knowledge base (internal context — solo root)
/memory/

# === JARVIS CORE MEMORY — Runtime nodes (efimeros, gitignored por diseno) ===
core/memory/agent_performance.json
core/memory/session_state.json

# Financial files
*.xlsx
*.xls
*.csv
agentes/contador/02_Archivos_Procesados/

# Real emails and documents
agentes/email_manager/inbox/
agentes/email_manager/drafts/
projects/*/facturas/
projects/*/recibos/

# === PYTHON ===
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.venv/
venv/
env/

# === OS & EDITOR ===
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
*.swp
*.swo

# === LOGS ===
logs/
*.log
